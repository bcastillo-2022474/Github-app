import { HoverParentDirective } from './hover-parent.directive';

describe('HoverParentDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverParentDirective();
    expect(directive).toBeTruthy();
  });
});
